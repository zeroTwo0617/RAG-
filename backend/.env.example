# 复制为 .env 并填写（.env 已被 .gitignore 忽略，不会提交）
# 数据库（docker-compose 默认）
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ragkb
DB_USERNAME=ragkb
DB_PASSWORD=ragkb

# Embedding：留空则自动使用本地 hash 兜底（无需任何 key 即可跑 M1）
EMBEDDING_PROVIDER=auto
EMBEDDING_API_BASE_URL=https://api.deepseek.com/v1/chat/completions
EMBEDDING_API_KEY=sk-3472726ef72c4a21ba0867eacd057460
EMBEDDING_API_MODEL=deepseek-v4-flash

# JWT（生产务必修改）
JWT_SECRET=change-me-in-production-please-1234567890
